# CoSMiT 项目重设计说明

## 1. 项目重新定义

CoSMiT 的研究对象不是“代码翻译”，而是“测试知识迁移”：给定目标深度学习框架中一个测试薄弱的关键组件，从一个或多个成熟源框架中找到功能相似组件，抽取其测试知识，在目标框架中重建并验证这些知识，最终形成可维护、可追溯、可扩展的目标测试。

一句话定义：

> CoSMiT 以组件关键性决定“测什么”，以多维相似性决定“向谁学习”，以测试意图和执行证据决定“迁移是否有效”。

项目名称统一为 `CoSMiT`，Python 包名和命令名统一为 `cosmit`。TransTest 仅作为本地冻结归档存在，不属于新项目源码。

## 2. 问题、输入与输出

### 2.1 核心问题

深度学习框架之间存在大量功能相似组件，但测试覆盖差异显著。重新编写测试成本高，并容易遗漏边界输入、dtype、设备、梯度、异常行为和数值误差等关键维度。成熟框架的测试仓库已经包含开发者积累的隐式知识，关键在于如何可靠地识别、抽取、迁移和验证这些知识。

### 2.2 系统输入

- 目标框架：组件源码、API 文档、现有测试、使用频率、提交记录、issue/缺陷记录。
- 源框架：组件源码、API 文档、调用样例、测试仓库和可重复执行环境。
- 运行配置：框架版本、Python/编译器版本、CPU/GPU、dtype 和后端信息。

### 2.3 系统输出

- 目标关键组件清单及可解释优先级。
- 源/目标组件对应关系及六维相似性证据。
- 结构化测试知识中间表示。
- 目标框架原生候选测试和完整转换记录。
- 静态、动态、差分、数值和意图保持验证证据。
- 排序后的有效测试、参数化增强测试和迁移报告。

## 3. 六阶段方法

### 3.1 关键组件识别

对目标组件计算五类信号：使用频率、测试薄弱程度、变更活跃度、历史缺陷敏感性和影响范围。任何总分都必须保留原始证据，避免把不可解释的单一分数当作结论。

测试薄弱程度至少由测试数量、断言多样性、输入变体、dtype、设备和梯度覆盖共同描述。影响范围应结合静态调用图和动态调用信息，而不是仅统计 import 次数。

### 3.2 相似组件检索

以名称、文档、参数结构、源码结构、调用上下文和测试行为六个维度检索 Top-K 源组件。不同组件类型采用不同权重：底层算子更重视参数和源码，高级训练接口更重视文档和调用上下文。

检索采用“高召回候选生成 + 证据重排”两步设计。最终映射允许一对多、多对一和近似对应，并显式记录不等价条件。

### 3.3 测试知识抽取

将源测试解析为六类结构单元：环境准备、输入构造、组件构造、执行、断言和清理。测试意图区分功能、数值、shape、dtype、设备、梯度、异常、边界和回归九类。

中间表示必须保留源码位置、组件调用、参数约束、随机性、预期行为和断言 oracle，确保后续生成可以回溯到原测试。

### 3.4 测试代码迁移

迁移包含五类显式变换：API/模块路径映射、参数对齐、输入数据适配、断言重构和测试基础设施适配。每次变换都形成记录，禁止无法解释的整段覆盖式生成。

生成结果必须是目标框架原生测试，并保留知识单元 ID、源测试位置和目标组件 ID。

### 3.5 验证与反馈修复

先执行语法、导入、符号、签名和明显类型检查，再在隔离环境动态运行。失败按编译/导入、参数、shape/layout、dtype/device、断言、数值、梯度、超时和环境分类。

只有明确属于可修复语义差异的失败才能进入有限轮次修复。每轮必须记录输入、诊断、修复动作和新执行结果；不可修复或破坏测试意图的候选进入丢弃集合。

### 3.6 排序与扩展

先检查目标组件调用、核心输入、核心断言和测试类别是否保留，再执行跨框架输出差分、梯度一致性和数值误差分析。

有效测试按测试价值、边界覆盖、dtype 覆盖、设备覆盖、缺陷发现概率和执行成本排序。扩展只作用于已验证的知识维度，包括 shape、dtype、设备和参数边界。

## 4. 研究问题

- **RQ1 关键性**：五维关键性排序能否比随机、使用频率单因子和覆盖率单因子发现更多真实测试缺口？
- **RQ2 相似性**：六维相似性在 Top-K 召回率、MRR 和映射准确率上是否优于名称、文档 embedding 或参数签名单一方法？
- **RQ3 知识保持**：显式测试知识中间表示能否提高迁移后的测试意图保持率和 oracle 完整率？
- **RQ4 闭环修复**：静态检查与有限轮次反馈修复分别对可执行率、有效率和成本产生多大贡献？
- **RQ5 测试增强**：迁移和扩展能为目标关键组件带来多少分支、边界、dtype、设备和梯度覆盖增益，并发现多少有效缺陷？

## 5. 首个研究切片

首个切片固定为 TensorFlow ↔ PyTorch 双向迁移，选择损失函数、张量算子和神经网络层三类组件。预实验每个方向先选 10-20 个目标组件，采用人工金标准验证关键性、相似映射、测试意图和最终有效性；正式实验再扩大组件与测试数量。

双向设置能够检验同一组件关系在不同迁移方向上是否对称，并覆盖函数式/对象式接口、参数默认值、shape/layout、数值容差、梯度和异常行为。领域模型仍保持框架无关，TensorFlow/PyTorch 差异封装在配置、映射规则与适配器中。

## 6. 贡献表述

建议将贡献收敛为三项：

1. 面向测试缺口的关键组件识别与六维组件相似性建模。
2. 保留测试结构、参数约束和测试意图的知识中间表示与可追溯迁移。
3. 结合执行修复、意图保持、差分数值验证和价值排序扩展的闭环。

贡献不应表述为“首次使用 LLM 生成测试”，也不应以“代码能运行”替代有效性。LLM 只是可替换实现部件，真正的方法贡献是结构化证据、约束和验证闭环。

## 7. 工程原则

- 框架无关核心与框架适配器分离。
- 六阶段只通过版本化 artifact 通信，不共享隐式全局文件。
- 所有评分保留分项证据、公式版本和配置快照。
- 所有生成测试可追溯到组件映射、知识单元和转换记录。
- 所有外部代码在隔离环境执行，设定资源、时间和网络边界。
- 未实现阶段明确标为未实现，不用占位结果冒充实验结果。

具体模块边界见 [系统架构](docs/architecture.md)，对照研究见 [相关工作](docs/related_work.md)，实验指标见 [实验设计](docs/evaluation.md)，实施顺序见 [路线图](docs/roadmap.md)。
